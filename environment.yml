name: env-glr

channels:
  - conda-forge
  - pytorch

dependencies:
  - python=3.11
  - pip

  # Core scientific python
  - numpy
  - pandas
  - matplotlib

  # Machine Learning
  - pytorch::pytorch
  - pytorch::torchvision
  - cudatoolkit=11.3

  # pip requirements for git hosted package
  - pip:
      - wheel
      - buildtools
      - torchviz
      - avalanche-lib
